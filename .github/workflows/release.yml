name: Build and Release

on:
  push:
    tags:
      - 'v*'          # runs whenever you push a tag like v1.0.0

permissions:
  contents: write     # needed so the workflow can create a Release

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact: PointCloudViewer-Windows.zip
          - os: macos-26
            artifact: PointCloudViewer-Mac-AppleSilicon.zip
          - os: macos-26-intel
            artifact: PointCloudViewer-Mac-Intel.zip
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r Requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        # Uses PointCloudViewer.spec instead of a raw --onefile/--add-data
        # command line -- see the comment at the top of that file for why
        # (short version: --add-data needs a different separator on Windows
        # than on macOS, and the .spec file sidesteps that entirely).
        run: pyinstaller PointCloudViewer.spec

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: |
          cd dist
          Compress-Archive -Path PointCloudViewer.exe -DestinationPath ../${{ matrix.artifact }}

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: |
          cd dist
          zip -r ../${{ matrix.artifact }} PointCloudViewer.app

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
